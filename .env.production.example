NODE_ENV=production

API_PORT=3000
COOKIE_NAME=ortaq_session
SESSION_SECRET=K6mY9pQ2vR8tN4xL1zD7wF3bH0sJ5cAqY2eM8uT1
# In production with Caddy proxying, CORS is usually handled by the proxy or allowed for same-origin via the true/empty config
CORS_ORIGIN=https://hesabat.vatgroup.az

SQLITE_DB_PATH=/data/sqlite/app.db
UPLOAD_DIR=/data/uploads
BACKUP_DIR=/data/backups

# VAPID keys for Push Notifications (generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# Only true if you want to allow running db seed in a production environment
ALLOW_PROD_SEED=false
# Change this before running seed in production
SEED_DEFAULT_PASSWORD=12345678